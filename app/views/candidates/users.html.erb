<script type="text/javascript">
    //document.getElementById("home_menu").style.borderBottom = "2px solid mediumseagreen";
    change_menu("user_menu")
</script>
<%= render '/page_title', :title => 'Users' %>
<%= link_to 'New', new_user_registration_path  %>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
      <tr>
        <td><%= user.full_name %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Edit'%> </td>
        <td><%= button_to "Delete", new_user_registration_path, {class: 'btn btn-xs btn-danger'} %></td>
      </tr>
  <% end %>
  </tbody>
</table>
